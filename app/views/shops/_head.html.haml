.head
  .head_left
    = link_to root_path,class:"top_page" do
      D-Ate...
  .head_right
    - if current_user
      .head_right__btn  
        = link_to new_shop_path,class:"btn" do
          = icon('fas', 'utensils') 
          new
      .head_right__btn    
        = link_to edit_user_registration_path,class:"btn" do
          = icon('fas', 'user')
          MyPage
      .head_right__btn   
        = link_to destroy_user_session_path, method: :delete, class:"btn" do
          = icon('fas', 'sign-out-alt')
          Logout
    - else
      .head_right__btn  
        = link_to new_shop_path,class:"btn" do
          = icon('fas', 'utensils') 
          new
      .head_right__btn    
        = link_to new_user_registration_path,class:"btn" do
          = icon('fas', 'user-edit')
          Join
      .head_right__btn
        = link_to new_user_session_path,class:"btn" do
          = icon('fas', 'sign-in-alt')
          Login
      